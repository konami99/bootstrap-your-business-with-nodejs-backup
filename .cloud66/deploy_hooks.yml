production:
staging:
  last_thing:
    - inline: |
        
        #!/usr/bin/env bash
        npm run migration:run
      target: node
      run_on: single_server
      execute: true
      apply_during: all
      destination: <%= ENV['STACK_PATH'] %>
